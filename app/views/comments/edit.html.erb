


<%= form_with model: @comment,url: article_comment_path(@comment.article_id,@comment.id),method:'put', local: true do |form| %>
<%# <%= form_with scope: :@article,article_path, local: true do |form| %> %>


  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@article.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :commenter %>
  </p>

  <p>
    <%= form.label :text %><br>
    <%= form.text_area :body %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>